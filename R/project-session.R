source(here::here("R/package-loading.R"))


1
#note to myself.

#How to assign a variable
weight_kilos <- 100
weight_kilos

# Example of a vector
c("a", "b")
c(1,2)
1:10

#Example of a dataframe
head(iris)

colnames(iris)
str(weight_kilos)
str(iris)
summary(iris)

#lm means linear function
lm


#You can insert a section under 'Code' --> insert section
# Section wrangling -------------------------------------------------------



# Exercise: Make code more readable ---------------------------------------

day_one
day1
T <- FALSE
c <- 9
mean <- function(x) sum(x)

# Spacing: There should not be a space before the comma but after the comma there should be a space
x[, 1]
x[, 1]
x[, 1]
mean(x, na.rm = TRUE)
mean(x, na.rm = TRUE)
function(x) {

}
function(x) {

}
height <- feet * 12 + inches
mean(x, na.rm = 10)
sqrt(x ^ 2 + y ^ 2)
df$z
x <- 1:10

# Indenting
if (y < 0 && debug) {
  message("Y is negative")
}


summing <- function(a,b) {
  add_numbers <- a + b
  return(add_numbers)
}

summing(2,2)

library(tidyverse)

write_csv(iris, here::here("data/iris.csv"))
iris_data <- read_csv(here::here("data/iris.csv"))
iris_data


# Data management ---------------------------------------------------------


